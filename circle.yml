machine:
  node:
    version: 8.0.0

general:
  artifacts:
    - ./coverage

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install redis-server
    - sudo service redis-server start
    - redis-cli ping

test:
  post:
    - bash <(curl -s https://codecov.io/bash)
